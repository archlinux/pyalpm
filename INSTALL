Requirements:

* Python 3 (at least version 3.1 for the C module, 3.2 for pycman)

* pacman 3.5 (with libalpm.so.6)

Installation instructions:

$ python3 setup.py build
$ python3 setup.py install --root=/usr

